#Progress

1. Card Class: Generierung der Symbolen für Karten, Erstellung einzelne Karten mit set/get Möglichkeit.
   prüft ob die neue Karte an die alte Karte gelegt werden kann, aber nur bei Wert. 
   Was kann schief laufen : überprüfe nummer, min 1 und max 13, Farbe. 
   Todo: Data typ check
   
2. Sequence Class: erwartet eine liste, es kann erste letzte karte ausgelesen werden, es kann neue karte 
   hinzugefügt werden , es können zwei sequnces zusammengeführt werden wenn Wert passt
   ToDo: für funktion append_card sollen cheks geschrieben werden , für fits in farbe check, merge cheks 
   bzw fits in benutzen, für sequenz full farbe check schreiben